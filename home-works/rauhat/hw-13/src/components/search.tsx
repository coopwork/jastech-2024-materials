import { Input} from "@/components/ui/input";

const Search_input = () => {
    return (
        <div className= "h-min ">
            <Input  placeholder="Search for cities" />
        </div>
    )}

    export default Search_input